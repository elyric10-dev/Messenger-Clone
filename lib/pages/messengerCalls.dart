import 'package:flutter/material.dart';

class MessengerCallsPage extends StatelessWidget {
  const MessengerCallsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Messenger Calls",
        style: TextStyle(fontSize: 40),
      ),
    );
  }
}
